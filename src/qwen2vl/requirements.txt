# Core
torch
torchvision
# transformers 5.x hard-requires torch>=2.5; base image ships torch 2.4.1.
# Pinned below 5.0 to avoid needing a lockstep torch+torchvision upgrade.
# >=4.57 is train.py's own stated minimum for Qwen3-VL support.
transformers>=4.57,<5.0
accelerate

# LoRA fine-tuning
peft

# Flash Attention (A100 optimized)
# Note: For Colab, use pre-built wheels in notebook for faster install
# For local: pip install flash-attn>=2.5.0 --no-build-isolation
# flash-attn>=2.5.0

# Data processing
datasets
pandas
numpy
pillow
scikit-learn
opencv-python-headless  # train.py imports cv2 unconditionally for augmentation - was missing entirely

# Utilities
pyyaml
tqdm

# Evaluation metrics
jiwer
evaluate